//  == media mixin
mw($media = 1700px, $above = false)
	if($above)
		@media (min-width $media)
			{block}
	else
		@media (max-width $media)
			{block}
			
mh($media = 600px, $above = false)
	if($above)
		@media (min-height $media)
			{block}
	else
		@media (max-height $media)
			{block}

// == other
btn()
	height 49px 
	border-radius 55px
	// background linear-gradient(180deg, #FF6D56 0%, #FF8835 100%)
	background-color #FDF10F
	text-transform uppercase
	font 700 14px/1 'FranklinGothic', Arial, Helvetica, sans-serif
	color #024CBD
	outline none
	transition all .5s ease
	cursor pointer
	border none
	&:hover
		// background rgba(255,137,53,1)
		// background -moz-linear-gradient(top, rgba(255,137,53,1) 0%, rgba(214,78,60,1) 100%)
		// background -webkit-gradient(left top, left bottom, color-stop(0%, rgba(255,137,53,1)), color-stop(100%, rgba(214,78,60,1)))
		// background -webkit-linear-gradient(top, rgba(255,137,53,1) 0%, rgba(214,78,60,1) 100%)
		// background -o-linear-gradient(top, rgba(255,137,53,1) 0%, rgba(214,78,60,1) 100%)
		// background -ms-linear-gradient(top, rgba(255,137,53,1) 0%, rgba(214,78,60,1) 100%)
		// background linear-gradient(to bottom, rgba(255,137,53,1) 0%, rgba(214,78,60,1) 100%)
		background-color #024CBD
		color #FDF10F


btn-tab()
	border-radius 100px
	border 1px solid #D2D2D2
	max-width 200px
	width 100%
	height 50px
	color #024CBD
	transition all .3s ease
	background #fff
	cursor pointer
	font-size 14px
	font-weight 700
	&:hover
		border-color #FDF10F
		background-color #FDF10F

		
box-style()
	border 1px solid #024CBD
	border-radius 10px
	transition all .3s ease
	// &:hover
	// 	background rgba(255, 136, 53, .7)
	// .active
	// 	background rgba(255, 136, 53, .7)




bg()
	background-size cover
	background-position center center
	background-repeat no-repeat
	
hover()
	background-color #fff
	-webkit-box-shadow 0px 0px 73px -20px rgba(0,0,0,0.75)
	-moz-box-shadow 0px 0px 73px -20px rgba(0,0,0,0.75)
	box-shadow 0px 0px 73px -20px rgba(0,0,0,0.75)

	

customScroll($w = 4px, $bgt = $color2, $b = 0px, $bg = $color )
	overflow auto
	@media (min-width:1241px)
		&::-webkit-scrollbar
			width $w

		&::-webkit-scrollbar-track
			background $bgt
			border $b
			
		&::-webkit-scrollbar-thumb
			background $bg
			
		-ms-overflow-style -ms-autohiding-scrollbar	